plugins {
    id "java-library"
    id "maven-publish"
}

description = 'gRPC: Context'

dependencies {
    runtimeOnly (project(":grpc-api")) {
        exclude group: "com.google.guava", module: "guava"
    }
}
